import React from 'react'
//import SessionTableViewOnly from '../common/SessionTableViewOnly'
import { apis } from '../../util'

const Sessions = (props) => {
  return (
    <>
      {/* <SessionTableViewOnly
        title={'Public Sessions'}
        sessionPageUrl={'/public/session?code='}
        api={apis.publicSessions}
      />
      <br></br> */}
    </>
  )
}

export default Sessions
